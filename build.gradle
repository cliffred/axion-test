buildscript {
    repositories {
        mavenLocal()
        jcenter()
    }
    dependencies {
        classpath 'pl.allegro.tech.build:axion-release-plugin:1.8.2-SNAPSHOT'
    }
}

apply plugin: 'java-library'
apply plugin: 'pl.allegro.tech.build.axion-release'

version = scmVersion.version